(print "What is your name? ")
(label name (getln))

(if (eq name "exit")
  (then (/exit))
  (else (println "heheh, " (car (.reverse name)) ".")))

# print "What is your name? "
# name = gets.chomp
#
# if name == "exit"
#   exit
# else
#   puts "heheh, " name.reverse, "."
# end
